apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    isDefault: true
    access: proxy
    database: ruuvi
    url: http://influxdb:8086
    jsonData:
      httpMode: GET

# datasources:
#   - name: InfluxDB_v1
#     type: influxdb
#     access: proxy
#     database: site
#     user: grafana
#     url: http://localhost:8086
#     jsonData:
#       httpMode: GET
#     secureJsonData:
#       password: grafana